const letters = new Set(["a","b","c"]);
//create a set

const myIterator = letters.entries();
// create an iterator

for (const entry of myIterator){
    console.log(entry);
}

// typeOf -  returns object
// instace of set - returns true.-= checks instance of any class

